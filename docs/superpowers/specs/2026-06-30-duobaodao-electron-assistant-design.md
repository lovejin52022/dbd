# 多宝岛 Electron 助手设计

日期：2026-06-30

## 目标

使用 Electron 构建一个仅前端的桌面助手。应用打开京东多宝岛页面，保留用户登录状态，让用户手动浏览和搜索商品，并把用户选中的商品详情页保存到本地抢单列表中，用于提醒和快速打开。

应用不会自动点击下单、出价或抢单按钮，不会绕过登录或验证码，也不会调用平台的非公开下单、出价或抢单接口。

## 范围

包含：

- 默认打开“我的页面”：
  `https://dbd.m.jd.com/ppdbd/pages/mine/index?scene=null`
- 识别当前页面是否为京东登录页：
  `https://plogin.m.jd.com/login/login`
- 允许用户在 Electron 窗口中手动完成登录。
- 通过 Electron 的正常 session 存储保留登录态。
- 提供“首页”快捷入口：
  `https://dbd.m.jd.com/ppdbd/paimai`
- 用户在内嵌页面中手动搜索商品并打开商品详情页。
- 识别当前 URL 是否为多宝岛商品详情页，并从中解析 `id`（拍卖品 ID）与 `skuid`（商品 SKU）。
- 在应用侧提供“加入抢单列表”操作，用于保存当前商品详情页。
- 本地保存抢单列表条目，包括标题、URL、加入时间、可选备注和可选期望价。
- 支持查看、编辑、删除和快速重新打开抢单列表条目。
- 基于页面可见文本和用户可控刷新提供保守的提醒能力。

不包含：

- 自动点击下单、出价或抢单按钮。
- 直接发送下单、出价或抢单接口请求。
- 绕过登录、绕过验证码、伪造设备指纹、生成请求签名或隐藏式自动化。
- 高频刷新或请求循环。

## 架构

项目使用 Electron、Vite 和 TypeScript。

主窗口分为三个区域：

- 顶部工具栏：导航和应用控制。
- 网页内容区：京东多宝岛移动页面。
- 抢单列表侧栏：本地商品条目和操作。

Electron 主进程负责 BrowserWindow、session 行为、桌面通知、文件存储和 IPC 处理。渲染进程负责工具栏、侧栏 UI 和本地交互状态。京东页面与应用 UI 保持隔离；应用只读取当前 URL、页面标题，以及提醒判断所需的有限可见文本。

## 导航

应用启动时加载：

`https://dbd.m.jd.com/ppdbd/pages/mine/index?scene=null`

如果导航到以下开头的 URL：

`https://plogin.m.jd.com/login/login`

工具栏显示“需要登录”状态。页面保持可用，用户可以在窗口中手动登录。登录成功后，应用恢复正常浏览状态并保留 session。

### 商品详情页识别

当内嵌 Webview 导航到多宝岛商品详情页时，应用应识别该 URL，并解析查询参数中的业务字段。

详情页 URL 模式（示例）：

`https://dbd.m.jd.com/ppdbd/pages/detail-v2/index?id=404150571&cprice=10&showhead=no&paimai=1&skuid=100243016865&scene=null`

识别规则：

- 主机名必须为 `dbd.m.jd.com`。
- 路径必须为 `/ppdbd/pages/detail-v2/index`（允许末尾带或不带 `/`）。
- 查询参数 `id` 必须存在且为非空字符串，表示拍卖品 ID（`auctionId`）。
- 查询参数 `skuid` 可选；存在时表示京东商品 SKU ID。

解析结果用于：

- 控制工具栏/侧栏中“加入抢单列表”按钮的可用状态（仅在详情页可点击）。
- 写入抢单列表条目的 `auctionId` 与 `skuid` 字段。
- 后续调用 `current_bid_info` 等只读接口时，可直接使用 `auctionId`。

实现位置：`src/shared/jd-url.js`（或等价共享模块），提供：

- `isProductDetailUrl(url: string): boolean`
- `parseProductDetailUrl(url: string): ProductDetailInfo | null`

其中 `ProductDetailInfo` 至少包含：

- `auctionId`：来自查询参数 `id`
- `skuid`：来自查询参数 `skuid`，缺失时为 `null`
- `url`：原始 URL 字符串

工具栏操作：

- 打开“我的页面”。
- 打开“首页”。
- 刷新当前页面。
- 在可以后退时返回上一页。
- 清除 session 并重新进入登录流程。
- 开关提醒。
- 开关窗口置顶。

## 抢单列表

当用户位于商品详情页时，可以点击应用 UI 中的“加入抢单列表”。应用记录：

- `id`：本地生成（与 URL 中的拍卖品 ID 无关）。
- `auctionId`：来自详情页 URL 查询参数 `id`。
- `skuid`：来自详情页 URL 查询参数 `skuid`；缺失时存为 `null`。
- `title`：当前页面标题；如果读取失败，则使用基于 URL 的兜底标题。
- `url`：当前详情页 URL。
- `addedAt`：ISO 时间戳。
- `note`：用户可选备注。
- `targetPrice`：用户可选期望价。
- `lastSeenText`：可选的最近一次提醒相关文本快照。
- `lastNotifiedAt`：可选的最近提醒时间，用于避免重复提醒。

侧栏支持：

- 打开条目。
- 编辑备注。
- 编辑期望价。
- 删除条目。
- 在有数据时显示最近检查时间或最近提醒时间。

存储使用 Electron `userData` 目录下的 JSON 文件。这样无需后端服务，也能在应用重启后保留数据。

## 提醒行为

提醒行为保持保守，并由用户控制。

应用可以检查当前打开页面，或用户打开的抢单列表条目页面中的可见文本。应用查找可配置关键词，例如：

- `抢单`
- `立即抢`
- `去抢`
- `可抢`
- `开拍`

当关键词出现时，应用可以：

- 显示桌面通知。
- 播放短提示音。
- 在用户开启时将窗口置前。
- 标记该条目已经在近期提醒过。

应用不会点击任何页面按钮。所有平台内操作都由用户手动完成。

如果第一版实现自动刷新，默认刷新间隔必须保守，至少为 30 秒，并且必须易于关闭。

## 错误处理

- 如果标题读取失败，保存 URL，并允许用户之后重命名或添加备注。
- 如果读取存储失败，从空列表开始，并尽量保留无法读取的原文件。
- 如果写入存储失败，在应用侧显示错误信息。
- 如果内嵌页面加载失败，显示失败 URL 并提供重新加载操作。
- 如果桌面通知权限或投递失败，仍保留应用内视觉提醒。

## 测试

第一版验证应包括：

- 应用启动后加载默认“我的页面”。
- 登录 URL 检测会改变工具栏状态。
- 商品详情页 URL（如 `detail-v2/index?id=...&skuid=...`）能被正确识别，并解析出 `auctionId` 与 `skuid`。
- 非详情页时“加入抢单列表”不可用；进入详情页后可用。
- “首页”快捷入口能导航到多宝岛首页。
- 刷新、后退和窗口置顶控制可用。
- 加入抢单列表后，条目会持久化到磁盘。
- 应用重启后抢单列表仍然存在。
- 编辑和删除操作会更新存储。
- 提醒关键词检测能触发应用内状态变化，并在可用时走桌面通知路径。
- 不存在自动点击下单按钮或发送下单、出价、抢单接口请求的代码路径。
